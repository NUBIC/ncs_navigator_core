.page_section
  .psc_registration
    %b
      Participant Calendar

  - if psc.is_registered?(@participant)
    = render "/participants/schedule", :participant => @participant
  - else
    Participant needs to be registered in PSC