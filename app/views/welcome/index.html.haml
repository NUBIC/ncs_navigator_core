- title = "Welcome"
    
- if @scheduled_activities
  .front_page_section.page_section
    - rows = @scheduled_activities['rows']
    - if rows.blank?
      %h4
        No Upcoming Activities Scheduled
    - else
      %h4
        Scheduled Activities
      %table.records
        %tr
          %th Date
          %th Person
          %th PPG Status
          %th Activity
        - rows.each do |row|
          - if row['subject']
            - person = Person.find_by_person_id(row['subject']['person_id'])
            - if person
              %tr{ :class => cycle('even_record', 'odd_record') }
                %td
                  = row['scheduled_date']
                %td
                  - if person
                    = link_to row['subject']['name'], person_path(person), :class => "show_link icon_link"
                  - else
                    = row['subject']['name']
                %td
                  - if person.participant
                    = person.participant.ppg_status
                  - else
                    n/a
                %td
                  = row['activity_name']
.front_page_section.page_section
  .column-container
    = link_to "Administer New Pregnancy Screener", start_pregnancy_screener_instrument_path, :class => "add_link icon_link"

.front_page_section.page_section
  .column-container
    = link_to "View All People", people_path, :class => "list_link icon_link"
  